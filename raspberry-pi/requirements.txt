# Install on the Pi using the OS package where possible.
Pillow>=10.0
numpy>=1.24
# Raspberry Pi 3B+ aarch64/Python 3.13 test runtime.
ai-edge-litert>=2.2
# On another architecture, replace this with a matching tflite-runtime wheel.
