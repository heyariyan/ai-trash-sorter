# Contributing

Develop one bounded milestone at a time. Keep hardware access behind interfaces, add or update tests with behavior changes, and do not commit secrets, downloaded datasets, device runtime data, or captured images.

Before committing, review `git status` and `git diff`. Use the project's conventional prefixes, such as `feat(camera):`, `feat(ai):`, `feat(sorter):`, `docs:`, and `test:`.
